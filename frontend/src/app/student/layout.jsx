import AppLayout from "@/components/shared/AppLayout";

export const metadata = {
  title: "Student — HostelFixIT",
};

export default function StudentLayout({ children }) {
  return <AppLayout>{children}</AppLayout>;
}
