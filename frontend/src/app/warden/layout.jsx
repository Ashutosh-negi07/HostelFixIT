import AppLayout from "@/components/shared/AppLayout";

export const metadata = { title: "Warden — HostelFixIT" };

export default function WardenLayout({ children }) {
  return <AppLayout>{children}</AppLayout>;
}
