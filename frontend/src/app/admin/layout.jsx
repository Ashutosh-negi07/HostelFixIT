import AppLayout from "@/components/shared/AppLayout";
export const metadata = { title: "Admin — HostelFixIT" };
export default function AdminLayout({ children }) { return <AppLayout>{children}</AppLayout>; }
