import AppLayout from "@/components/shared/AppLayout";
export const metadata = { title: "Worker — HostelFixIT" };
export default function WorkerLayout({ children }) { return <AppLayout>{children}</AppLayout>; }
